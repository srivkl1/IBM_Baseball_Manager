# Optional real-data packages. pybaseball pins pandas<2.2 so it currently
# only installs on Python ≤ 3.13 (no wheels for 3.14 yet). The app gracefully
# falls back to a synthetic dataset when these are missing, so you can still
# run the full UI on Python 3.14 without these installed.
pybaseball>=2.2.7
espn-api>=0.35.0
