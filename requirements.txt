# Core - works on Python 3.10-3.14 with prebuilt wheels on major OSes.
streamlit>=1.31
python-dotenv>=1.0
pandas>=2.1,<2.2
numpy>=1.26
scikit-learn>=1.3
requests>=2.31
plotly>=5.20
joblib>=1.3

# Streamlit Cloud installs only this file by default. Keep watsonx here so
# LLM_PROVIDER=watsonx does not fall back to mock in the hosted app.
ibm-watsonx-ai>=1.1.0

# Needed for hosted users to paste ESPN league credentials in the sidebar.
espn-api>=0.35.0

# Real advanced baseball data. Without this, hosted ranking prompts should
# refuse synthetic/fake rankings instead of inventing teams/projections.
pybaseball>=2.2.7
